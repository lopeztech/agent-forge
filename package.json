{
  "name": "agent-forge",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Platform for autonomous, long-running software product teams composed of Claude agents.",
  "engines": {
    "node": ">=22.6.0"
  },
  "scripts": {
    "test": "node --experimental-strip-types --test tests/**/*.test.ts",
    "typecheck": "tsc --noEmit",
    "onboard:github-apps": "node --experimental-strip-types scripts/onboard-github-apps.ts",
    "seed:labels": "node --experimental-strip-types scripts/seed-labels.ts",
    "seed:product": "node --experimental-strip-types scripts/seed-product.ts",
    "smoke:github-app": "node --experimental-strip-types scripts/smoke-github-app.ts"
  },
  "dependencies": {
    "@aws-sdk/client-bedrock-runtime": "^3.1048.0",
    "@aws-sdk/client-dynamodb": "^3.1048.0",
    "@aws-sdk/client-eventbridge": "^3.1048.0",
    "@aws-sdk/client-s3": "^3.1053.0",
    "@aws-sdk/client-secrets-manager": "^3.700.0",
    "@aws-sdk/client-sfn": "^3.1053.0",
    "@aws-sdk/lib-dynamodb": "^3.1048.0",
    "yaml": "^2.9.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "esbuild": "^0.28.0",
    "typescript": "^5.7.0"
  }
}
